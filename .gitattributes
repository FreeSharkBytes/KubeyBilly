# Normalize line endings to LF for all text files.
* text=auto eol=lf

# Binary files, no text conversion.
*.png binary
*.jpg binary
*.jpeg binary

# Diagram sources and renders are text.
*.svg text
*.puml text

# Evidence fixtures are byte-exact captures; the manifest sha256 must keep matching.
test/fixtures/incidents/** -text
