apiVersion: v1
kind: Service
metadata:
  name: {{ include "kubeybilly.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "kubeybilly.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "kubeybilly.selectorLabels" . | nindent 4 }}
