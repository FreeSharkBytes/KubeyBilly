# Elixir / Phoenix build artifacts (the Phoenix skeleton lands later,
# these ignores are here waiting for it).
/_build/
/deps/
*.ez
erl_crash.dump
/.elixir_ls/
/cover/
/doc/
/.fetch
kubeybilly-*.tar

# Phoenix assets
/node_modules/
/assets/node_modules/
/priv/static/assets/
/priv/static/cache_manifest.json

# Secrets and local config
.env
.env.*

# Runtime output: incident bundles are evidence, never source.
/incidents/

# OS junk
Thumbs.db
.DS_Store
